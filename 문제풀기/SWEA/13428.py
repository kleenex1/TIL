import sys

sys.stdin = open("_숫자조작.txt")


T = int(input())

for test_case in range(1, T+1):
    nums = input()

    # Max:같은 숫자가 있으면 인덱스가 뒤에 있는 것을 맨 앞에 숫자랑 바꾼다.
    # Min:같은 숫자가 있으면 인덱스가 뒤에 있는 것을 맨 앞에 숫자랑 바꾼다.



